:releasehx_version: {{ version }}
:htmlpreview_base_url: https://htmlpreview.github.io/?https://raw.githubusercontent.com/DocOps/releasehx-demo/generated/latest/artifacts

= ReleaseHx {releasehx_version} Demo Artifacts
:toc:
:toclevels: 2
:icons: font

== Overview

This branch contains demonstration artifacts generated by ReleaseHx {releasehx_version}.

Browse the examples below to see ReleaseHx in action across different platforms and configurations.

Source::
 See the https://github.com/DocOps/releasehx-demo[main branch] for configs, payloads, and documentation

== Featured Examples 🌟

Start with these to quickly evaluate ReleaseHx capabilities.

{% for combo in featured_combinations %}
=== {{ combo.desc }}

{%- assign first_version = combo.versions.first %}
{%- assign artifacts = combo.artifacts[first_version] %}

{%- if artifacts.files.html %}
*link:{htmlpreview_base_url}/{{ combo.slug }}-{{ first_version }}.html[{{ combo.slug }}-{{ first_version }}.html]* (Open in browser) +
link:https://github.com/DocOps/releasehx-demo/blob/generated/latest/artifacts/{{ combo.slug }}-{{ first_version }}.html[HTML source^]
{%- endif %}

{%- if artifacts.change_count > 0 %}
* {{ artifacts.change_count }} change{% if artifacts.change_count != 1 %}s{% endif %}{% if artifacts.note_count > 0 %} with {% if artifacts.note_count == artifacts.change_count %}comprehensive{% else %}{{ artifacts.note_count }}{% endif %} metadata{% endif %}
{%- endif %}
{%- if combo.showcase.highlights %}
{% for highlight in combo.showcase.highlights %}
* {{ highlight }}
{% endfor %}
{%- endif %}

Also available::
{% for format in combo.file_order %}
{%- assign file_info = artifacts.files[format] %}
{%- if file_info %}
{% unless forloop.first %} | {% endunless %}link:artifacts/{{ combo.slug }}-{{ first_version }}.{{ format }}[{% if format == 'pdf' %}PDF{% elsif format == 'md' %}Markdown{% elsif format == 'adoc' %}AsciiDoc{% elsif format == 'yaml' %}YAML{% else %}{{ format | upcase }}{% endif %}{% if file_info.size_human %} ({{ file_info.size_human }}){% endif %}]
{%- endif %}
{%- endfor %}

{% endfor %}

== Configuration Variations

Browse additional configuration examples showing different layout options and platform capabilities.

{%- assign github_combos = '' | split: '' %}
{%- assign gitlab_combos = '' | split: '' %}
{%- assign jira_combos = '' | split: '' %}
{%- assign generic_combos = '' | split: '' %}

{%- for combo in combinations %}
{%- unless combo.showcase %}
  {%- if combo.platform == 'github' %}
    {%- assign github_combos = github_combos | push: combo %}
  {%- elsif combo.platform == 'gitlab' %}
    {%- assign gitlab_combos = gitlab_combos | push: combo %}
  {%- elsif combo.platform == 'jira' %}
    {%- assign jira_combos = jira_combos | push: combo %}
  {%- elsif combo.platform == 'generic' %}
    {%- assign generic_combos = generic_combos | push: combo %}
  {%- endif %}
{%- endunless %}
{%- endfor %}

{%- if github_combos.size > 0 %}
=== GitHub Examples

{%- for combo in github_combos %}
{%- assign first_version = combo.versions.first %}
* link:artifacts/{{ combo.slug }}-{{ first_version }}.html[{{ combo.desc }}]
{% endfor %}
{%- endif %}

{%- if gitlab_combos.size > 0 %}
=== GitLab Examples

{% for combo in gitlab_combos %}
{%- assign first_version = combo.versions.first %}
* link:artifacts/{{ combo.slug }}-{{ first_version }}.html[{{ combo.desc }}]
{% endfor %}
{%- endif %}

{%- if jira_combos.size > 0 %}
=== Jira Examples

{% for combo in jira_combos %}
{%- assign first_version = combo.versions.first %}
* link:artifacts/{{ combo.slug }}-{{ first_version }}.html[{{ combo.desc }}]
{% endfor %}
{%- endif %}

{%- if generic_combos.size > 0 %}
=== Generic Platform

{% for combo in generic_combos %}
* {% for version in combo.versions %}
{% unless forloop.first %}, {% endunless %}link:artifacts/{{ combo.slug }}-{{ version }}.html[v{{ version }}]
{%- endfor %} ({{ combo.desc }})
{% endfor %}
{%- endif %}

== All Generated Files

Browse the link:artifacts/[artifacts/] directory for all generated files:

* *YAML* (Structured RHYML intermediate format)
* *Markdown* (GitHub-compatible with inline badges)
* *AsciiDoc* (Semantic markup with role attributes)
* *HTML* (Wrapped, Bootstrap 5, semantic HTML5)
* *PDF* (Publication-ready via asciidoctor-pdf)

== Key Features Demonstrated

=== Metadata Display

* Type badges (Feature, Bug, Improvement)
* Part badges (UI, API, Security, Performance)
* Tag badges (enhancement, breaking, security, etc.)
* Ticket links with issue numbers
* User assignments
* Git commit references

=== Layout Flexibility

* Grouped release notes by category
* Changelog-only or notes-only layouts
* Multiple frame styles (cards, lists, tables, admonitions)
* Custom type/part ordering
* Dark mode and custom CSS support

== Documentation & Source

* *Main README*: https://github.com/DocOps/releasehx-demo[View on GitHub]
* *ReleaseHx docs*: https://releasehx.docopslab.org/docs
* *Configuration examples*: See main branch link:https://github.com/DocOps/releasehx-demo/tree/main/_configs[`_configs/`] directory
* *Payload examples*: See main branch `_payloads/` directory

---

This is an artifacts-only branch with no shared history with `main`. +
For source files and documentation, see the https://github.com/DocOps/releasehx-demo[main branch].

