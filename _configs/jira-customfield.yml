origin:
  source: jira
  label: Jira Cloud API
  format: json

paths:
  drafts_dir: drafts
  payloads_dir: issues
  templates_dir: _templates

conversions:
  summ: issue_heading
  note: custom_field
  note_custom_field: customfield_10039
  markup: markdown

parts:
  webui:
    head: Web UI
    icon: desktop
    text: Web UI
  api:
    head: API
    icon: server
    text: API
  installer:
    head: Installer
    icon: cloud-download
    text: Installer

links:
  web: https://jira.example.com/browse/{{ tick }}
  git: https://github.com/DocOps/releasehx-demo/commit/{{ hash }}
  usr: https://example.com/staff/profiles/{{ vars.lead }}

modes:
  markdown_frontmatter: true
  html_wrap: true
  asciidoc_frontmatter: true

rhyml:
  pasterize_head: true
  chid: "{{ release.code | replace: '.', '_' | upcase }}-{{ change.tick }}"

history:
  title: 'Release History: Jira Custom Field'
  markdown_frontmatter: |
    ---
    title: "{{ release.code }} Release Notes"
    version: "{{ release.code }}"
    date: "{{ release.date }}"
    permalink: /artifacts/jira-customfield-{{ release.code }}_from-md_/
    layout: artifact-markdown
    ---
  html_framework: bootstrap5
  items: 
    empty_groups: |
      No changes in this {{ group_type }} category.

changelog:
  spot: 1
  items:
    frame: paragraph

notes:
  spot: 2
  items:
    frame: table-cols-2
    allow_redundant: true

tags:
  changelog:
    incl: changelog
  breaking:
    incl: breaking
    note: "⚠️ **Breaking Change**"
  highlighted:
    incl: highlighted

types:
  feature:
    head: Features
    icon: star
  improvement:
    head: Improvements
    icon: arrow-up
  bug:
    head: Bug Fixes
    icon: bug
  task:
    head: Tasks
    icon: check
